# Philippines v16 identity migration

Philippines v16 inherits the complete Philippines v15 canonical package and
retained evidence. The portable case changes only its directory identity,
`osy-casename`, local README, and empty result registry. Every editable model
parameter file is byte-for-byte unchanged, and generated solver inputs and
results are excluded. Git tag `v15.0.0` preserves the numerical predecessor.
