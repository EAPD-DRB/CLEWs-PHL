# Generic physical-gate interpretation

The standard optimistic source gate passes for BASE, COAL_PHASEOUT, RE and EV.

An additional diagnostic was run with `--historical-through 2024`. It failed on 2021 inherited heat-service rows because that option deliberately excludes finite 2021-2024 endogenous investment envelopes from optimistic supply. The same option fails unchanged, source-identical `Philippines_v23` on the same rows (330 failures), while the retained v23 LP has a proven optimum. It is therefore not the correct boundary for this 2020-2053 endogenous-investment model.

The four failed v24 reports and the unchanged-v23 cross-check are retained outside the clean candidate at `WebAPP/DataStorage/.Philippines_v24-agriculture-diagnostics-20260825/`. No optimizer was run. The release candidate contains only the four standard passing reports.
