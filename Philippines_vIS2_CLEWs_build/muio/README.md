# Portable MUIO case

`Philippines_vIS2_vIS2.0.0_MUIO.zip` is the editable Philippines vIS2 case.
Extract it as `Philippines_vIS2` under MUIOGO's `WebAPP/DataStorage/` directory.

The archive is intentionally result-free. Regenerate scenario matrices and
results through the MUIOGO application workflow.
